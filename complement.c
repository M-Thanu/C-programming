#include <stdio.h>

int main(){
    int num;
    char Snum[10];

    /*printf("Enter the binary number: ");
    scanf("%d", &num);

    sprintf(Snum, "%d", num);

    for(int i=0; i<c; i++){
        printf("%c \n",Snum[i]);
    }*/

    return 0;
}